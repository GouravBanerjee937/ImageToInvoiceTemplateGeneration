services:
  - type: web
    name: imagetoinvoice
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn 'Mapping fields:app'"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: OPENAI_API_KEY
        sync: false
